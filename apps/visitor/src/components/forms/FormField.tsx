import type { ComponentType } from "react";
export function FormField({label,value,onChange,placeholder,icon:Icon,error}:{label:string;value:string;onChange:(value:string)=>void;placeholder:string;icon?:ComponentType<{size?:number;className?:string}>;error?:string}){return <label className="grid gap-1.5"><span className="font-medium">{label}</span><span className="relative"><input value={value} onChange={(event)=>onChange(event.target.value)} placeholder={placeholder} className={`h-11 w-full rounded-[10px] bg-white px-3 text-sm outline-none ring-1 ring-inset ring-[var(--np-line)] focus:ring-2 focus:ring-[var(--np-brand)] ${Icon?"pl-10":""}`}/>{Icon&&<Icon size={18} className="absolute left-3 top-1/2 -translate-y-1/2 text-[var(--np-muted)]"/>}</span>{error&&<span className="text-xs text-rose-600">{error}</span>}</label>}
